class heights {
        homeHeight;
        aboutHeight;
        portfolioHeight;
        contactHeight;
}

function updateHeight(file, newHeight) {
        console.log(newHeight);
        homeHeight = newHeight;
        console.log(homeHeight);
}